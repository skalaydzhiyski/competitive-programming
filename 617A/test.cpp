#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <cstdio>

int main(int argc, const char** argv) {
  // that is what we need here brother 
  int x;
  scanf("%d", &x);
  printf("%d", x);
  return 0;
}
