#include <iostream>
using namespace std;

int main(void) {
  /* Solution for this problem consists of finding the interval 
   * in which our target value lies using the numbers in the array
   * i.e. -> [a[i], a[i]**2] until a[i]**2 becomes bigger than the target value
   * and then run binary search in the given interval to get O(log(N)) running time
  */
  return 0;
}

