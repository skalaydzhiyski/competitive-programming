#include <iostream>
#include <string>
using namespace std;

string solve(const string& str, int key) {
  int n = str.size();
  for (int i=0; i<n; ++i) {
    strcat(res, (char) (int) str[i]);
  }
  return res;
}

int main(void) {
  string a = "xyz";
  int k = 3;
  cout << solve(a, k) << endl;
  return 0;
}

